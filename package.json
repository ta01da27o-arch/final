{
  "name": "boat-ai-fetcher",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "fetch": "node server/fetch_today_full.js",
    "install-browsers": "playwright install --with-deps"
  },
  "dependencies": {
    "playwright": "^1.41.2"
  }
}